import 'package:flutter/material.dart';
import 'package:spacex/pages/index.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Index(),
    );
  }
}
